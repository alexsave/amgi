{
  "name": "plusaudio",
  "private": true,
  "description": "Node CLI that adds generated audio to an existing Anki deck without changing its identity. Deliberately kept out of the web app's dependency tree.",
  "engines": {
    "node": ">=26.1.0"
  },
  "scripts": {
    "test": "node --test test/"
  },
  "dependencies": {
    "openai": "^4.79.4"
  }
}
